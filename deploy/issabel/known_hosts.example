# Replace this file with the pinned SSH host key collected from the Issabel server.
# Example (run from a trusted machine on the company LAN):
# ssh-keyscan -p 22 192.0.2.10 > deploy/issabel/known_hosts
